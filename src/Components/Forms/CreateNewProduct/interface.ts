export interface CreateNewProductValues {
  productName: string;
  productBrand: string;
  productID: string;
  productCategroy: string;
  productDiscription: string;
  productCount: string;
  productPrice: string;
  // productImage: string,
}
